# Coinsdance 릴리스 노트

## 1.0.9 (versionCode 10) — 발행 전 미리보기

- 등록 코인 화면을 카드와 다단 필터 UI에서 엑셀형 6열 표로 단순화합니다.
- 타이틀 행은 `코인명`, `보유유무`, `매수대상`, `총매수원가`, `현재평가액`, `등락률`로 구성합니다.
- 보유유무와 매수대상은 각각 `전체/유/무`를 독립적으로 선택할 수 있습니다.
- 세 숫자 열을 동시에 정렬할 수 있으며 헤더의 숫자가 정렬 우선순위, 화살표가 방향을 나타냅니다.
- `MainActivity`에서 모든 화면과 뷰를 제거하고 앱 셸, 공용 컴포넌트, 현황·코인·거래·설정·인증 화면 파일로 분리합니다.
- 모든 화면에 샘플 데이터 기반 Compose Preview를 추가합니다.

### Firebase App Distribution용

등록 코인 화면을 엑셀처럼 단순한 표로 변경했습니다. 보유 여부와 매수 대상 여부를 각 헤더에서 선택하고, 매수원가·평가액·등락률은 여러 열을 조합해 정렬할 수 있습니다.

## 1.0.8 (versionCode 9)

- 등록 코인 화면에서 중복 정보인 `KRW-코인` 마켓 문자열을 제거하고 코인 심볼만 표시합니다.
- 각 등록 코인에 보유 여부, 신규 매수 대상 여부, 총매수원가, 현재평가액과 등락률을 표시합니다.
- 코인 심볼 검색, 보유·매수대상·등락 상태, 최소 매수원가와 최소 평가액 필터를 제공합니다.
- 코인, 보유 여부, 매수 대상 여부, 총매수원가, 현재평가액과 등락률을 각각 오름차순·내림차순으로 정렬할 수 있습니다.
- 종목별 금액과 등락률은 한 조회 주기의 업비트 실잔고, 공식 매수평단가와 현재가로 계산합니다.

### Firebase App Distribution용

등록 코인 화면을 전면 개선했습니다. 코인별 보유·매수 대상 상태와 총매수원가, 현재평가액, 등락률을 한눈에 보고 모든 항목을 필터링하거나 정렬할 수 있습니다. 모든 거래가 원화 마켓이므로 불필요한 `KRW-` 표시는 제거했습니다.

## 1.0.7 (versionCode 8)

- 매수·매도 추적 카드의 본 숫자는 목표가 달성 후 실제 저점·고점을 추적 중인 종목 수로 표시합니다.
- 괄호 안 숫자는 보유 중인 전체 코인 가운데 현재가가 업비트 공식 매수평단가보다 낮거나 높은 종목 수로 단순화합니다.
- 앱이 foreground로 복귀할 때 서버 연결 상태와 연속 실패 횟수를 초기화하고 즉시 상태를 다시 조회합니다.
- resume·로그아웃·인증 전환에 따른 정상적인 요청 취소를 연결 실패로 잘못 계산하던 문제를 수정합니다.
- 실제 통신 실패가 3회 연속 발생하기 전에는 `봇 상태 확인 중`을 유지합니다.
- 서버 거래내역 조회 범위를 확대하고 거래 장부를 누적하여 다수의 IOC 미체결 취소 때문에 실제 체결내역이 한 건만 표시되던 문제를 수정합니다.

### Firebase App Distribution용

앱 재개 과정의 정상적인 요청 취소를 연결 장애로 표시하지 않도록 수정했습니다. 실제 연결 실패가 3회 연속 확인될 때만 장애를 표시하며, 거래내역이 한 건만 보이던 서버 조회 문제도 함께 해결했습니다.

## 1.0.6 (versionCode 7)

- Google 인증 상태와 CoinSDance 서버 연결 상태를 완전히 분리합니다.
- 인증 만료·무효 사용자·HTTP 401/403은 연결 장애로 오인하지 않고 로그인 화면으로 이동합니다.
- 서버 연결 상태의 기본값을 항상 `확인 중`으로 설정합니다.
- 일시적 연결 실패의 재시도 중에는 `확인 중`을 유지하고 3회 연속 실패할 때만 연결 오류를 표시합니다.
- 이전 서버 응답의 봇 장애 값이 새 연결 확인 중 잘못 노출되지 않도록 상태 우선순위를 수정합니다.
- 앱 resume마다 연결 상태를 `확인 중`, 실패 횟수를 0으로 초기화하고 즉시 다시 조회합니다.
- resume·로그아웃·인증 전환에 따른 정상적인 요청 취소를 연결 실패로 계산하던 문제를 수정합니다.
- 실제 연결 실패가 정확히 3회 연속 발생해야만 오류를 표시하도록 상태 전이 테스트를 추가합니다.
- 거래내역의 마켓명에서 불필요한 `KRW-` 접두사를 제거하고 코인 심볼만 표시합니다.
- 서버와 API에서는 정확한 마켓 식별을 위해 기존 `KRW-코인` 값을 그대로 유지하며 앱 표시만 간결하게 변경합니다.

### Firebase App Distribution용

로그인 상태와 서버 연결 상태를 분리하여 앱 실행 직후 또는 인증 갱신 과정에서 연결 장애가 잘못 표시되는 문제를 수정했습니다. 인증이 만료되면 로그인 화면으로 이동하며, 서버는 실제 연결 실패가 확인된 경우에만 오류로 표시됩니다. 거래내역의 마켓명은 `KRW-B3` 대신 `B3`처럼 코인 심볼만 간결하게 표시합니다.

## 1.0.5 (versionCode 6)

- Firebase Credential Manager 기반 Google 로그인을 추가합니다.
- 로그인 세션이 없으면 Google 로그인 전용 화면을 먼저 표시하고, 성공 후에만 메인 대시보드로 이동합니다. 기존 세션이 있으면 메인 화면으로 바로 진입합니다.
- 서버 주소와 공용 인증 토큰 입력을 제거하고 Firebase ID 토큰으로 서버에 자동 연결합니다.
- 로그인된 Google 계정을 설정 화면에서 확인하고 로그아웃할 수 있습니다.
- 업비트 API 키 갱신도 동일한 Google 인증으로 보호합니다.
- 향후 Google Play App Signing 인증서 지문을 Firebase에 추가하면 같은 로그인 구현을 그대로 사용할 수 있습니다.

### Firebase App Distribution용

서버 주소나 인증 토큰을 직접 입력할 필요 없이 허용된 Google 계정으로 로그인하면 CoinSDance에 자동 연결됩니다. 앱 재설치 후에도 Google 계정으로 다시 로그인할 수 있으며, 허용되지 않은 계정은 서버에서 차단됩니다.

## 1.0.4 (versionCode 5)

- Android 런처와 앱 상단의 사용자 표시 이름을 `Coinsdance`로 통일합니다.
- 매수·매도 추적 수 옆 괄호에 현재 실제 저점·고점 훅이 형성된 종목 수를 숫자로 표시합니다.
- 앱 시작과 일시적인 통신 실패 후 자동 재시도 중에는 `봇 상태 확인 중`을 표시합니다.
- 5초 간격 조회가 3회 연속 실패한 경우에만 연결 장애를 표시합니다.
- 정상 응답을 받으면 실패 횟수를 즉시 초기화하고 정상 상태로 복귀합니다.
- 서버가 정상 응답으로 봇 장애를 명시한 경우에는 즉시 장애를 표시합니다.

### Firebase App Distribution용

매수·매도 추적 카드에서 실제 저점·고점 훅 형성 수를 괄호 숫자로 함께 확인할 수 있습니다. 앱 시작 및 통신 재시도 중에는 정상 봇이 장애로 잘못 표시되지 않도록 상태 판정을 개선했으며, 3회 연속 통신 실패 또는 서버가 확인한 실제 장애에만 장애 상태를 표시합니다.

## 1.0.3 (versionCode 4)

- 등록 코인 목록의 모든 종목에 `보유` 또는 `미보유` 상태를 명확히 표시합니다.
- 현황 화면의 신규 매수 대상 카드 옆에 현재 `최소 1회 매수금액`을 표시합니다.
- 최소 매수금액은 서버의 실제 종목별 배정액과 첫 매수 비중(`1/45`)으로 계산되며, 배정액 변경 시 자동으로 갱신됩니다.

### Firebase App Distribution용

등록 코인별 보유 여부를 명확히 표시하도록 개선했습니다. 현황 화면에는 현재 배정액을 기준으로 계산한 최소 1회 매수금액이 추가되며, 서버의 배정액 변경에 맞춰 자동 갱신됩니다.
