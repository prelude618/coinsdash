# CoinSDash 릴리스 노트

## 1.0.3 (versionCode 4)

- 등록 코인 목록의 모든 종목에 `보유` 또는 `미보유` 상태를 명확히 표시합니다.
- 현황 화면의 신규 매수 대상 카드 옆에 현재 `최소 1회 매수금액`을 표시합니다.
- 최소 매수금액은 서버의 실제 종목별 배정액과 첫 매수 비중(`1/45`)으로 계산되며, 배정액 변경 시 자동으로 갱신됩니다.
- 앱 시작과 일시적인 통신 실패 후 자동 재시도 중에는 `봇 상태 확인 중`을 표시하며, 3회 연속 실패했을 때만 연결 장애를 표시합니다.

### Firebase App Distribution용

등록 코인별 보유 여부와 현재 배정액 기준 최소 1회 매수금액을 표시합니다. 앱 시작 및 통신 재시도 중 정상 봇이 장애로 잘못 표시되지 않도록 상태 판정을 개선했습니다.
